// 使っておらずReferenceError: store is not definedになるのでコメントアウト
// import createPersistedState from 'vuex-persistedstate'

// // クライアントサイドでのみ実行されるように、process.client を使用して確認
// if (process.client) {
//   window.onNuxtReady(() => {
//     createPersistedState({
//       // 設定オプション
//     })(store)
//   })
// }
